#pragma once

#include <string>

/**
 * Sets up native macOS menu bar for the application
 * 
 * @param appName The name of the application to display in menu items
 */
void setupMacOSMenu(const std::string& appName);
